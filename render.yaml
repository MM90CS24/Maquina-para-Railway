services:
  - type: web
    name: Toxic-MD
    env: docker
    region: Frankfurt
    plan: free
    buildCommand: "apt-get neofetch"
    startCommand: "sshx"
    envVars:
      - key: PORT
        value: "3000"
