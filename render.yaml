services:
  - type: web
    name: Toxic-MD
    env: docker
    region: Frankfurt
    plan: free
    buildCommand: "apt install neofetch"
    startCommand: "sshx"
    envVars:
      - key: PORT
        value: "3000"
